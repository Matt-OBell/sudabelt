<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="email_template_hr_reminder_confirmation_date_1" model="mail.template">
            <field name="name">HR Confirmation Reminder Template</field>
            <field name="email_from">${(user.email or '')}</field>
            <field name="subject">${user.company_id.name} Birthday Reminder</field>
            <field name="email_to">${object.hr_email}</field>
            <field name="lang">${object.lang}</field>
            <field name="model_id" ref="hr.model_hr_employee"/>
            <field name="auto_delete" eval="True"/>
            <field name="body_html"><![CDATA[
<div style="font-family: 'Lucica Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px; color: rgb(34, 34, 34); background-color: rgb(255, 255, 255); ">

    <p>Hello HR Department</p>
    <p>

This is auto generated email by the system regarding reminder of confirmation of employee ${object.name} on ${object.confirm_date}.

Thanks,
${user.name}
${user.company_id.name}
    </p>
<br/>
</div>

]]></field>
        </record>

        <record forcecreate="True" id="ir_cron_confirmation_reminder_action" model="ir.cron">
            <field name="name">Confirmation Date Reminder</field>
            <field eval="True" name="active"/>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field eval="False" name="doall"/>
            <field eval="'hr.employee'" name="model"/>
            <field eval="'run_confirmation'" name="function"/>
            <field eval="'()'" name="args"/>
        </record>

    </data>
</odoo>
